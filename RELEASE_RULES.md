# リリースおよび開発運用ルール (Release & Development Rules)

本リポジトリにおける開発、リリース作業、およびコミュニケーションにおいて、以下のルールを絶対遵守すること。本ルールに違反する行為（不注意によるビルドエラーの誘発、無断の仕様変更、指示と異なる言語での応答など）は二度と繰り返してはならない。

---

## 1. リリースバージョン管理の絶対ルール

Gitタグ（例：`v1.34`）の作成およびプッシュを行う際は、パッケージメタデータとタグのバージョン情報に一切の齟齬が生じないよう、以下の手順を厳格に実行すること。

### 1.1 事前更新対象ファイル
タグを打つ前に、必ず以下のファイルのバージョン文字列を、リリースするバージョンと完全に一致するように更新すること。

1. **[pyproject.toml](pyproject.toml)**
   * `[project]` テーブル内の `version` フィールド
   ```toml
   version = "1.34" # リリースバージョンと一致させる
   ```
2. **[__init__.py](__init__.py)** (リポジトリルート)
   * `__version__` 変数
   ```python
   __version__ = "1.34" # リリースバージョンと一致させる
   ```

### 1.2 リリース手順の順序
バージョン更新とデプロイの順序は以下を厳守し、**コミット・プッシュが完了してからタグを作成すること。**

1. `pyproject.toml` および `__init__.py` のバージョン文字列を更新。
2. バージョン更新のみ、あるいはドキュメント修正を含めた変更をコミット。
3. 変更をリモートリポジトリの `main` ブランチへプッシュ (`git push origin main`)。
4. **プッシュ完了後に**、ローカルでタグを作成 (`git tag v1.XX`)。
5. 作成したタグをリモートへプッシュ (`git push origin v1.XX`)。

> [!IMPORTANT]
> バージョン表記の同期を怠ったままタグのみを先行してプッシュすることは、GitHub Actions による自動ビルド・デプロイエラー（Publish to Comfy registry ワークフローの失敗）を招くため、いかなる場合も禁止とする。

---

## 2. ユーザーコミュニケーションの絶対ルール

### 2.1 応答言語の制限
* ユーザーからの指示が日本語である場合、チャット上での返答・進捗報告・謝罪等は**すべて日本語のみ**で行うこと。
* ドキュメントやコードコメントの作成指示において明示的に英語が指定された場合のみ、その成果物内部で英語を使用し、対話コンテキストは常に日本語を維持すること。

### 2.2 言い訳および責任転嫁の禁止
* 不具合やビルドエラーが発生した際、「自分は書き換えていない」「既存のコードがこうだった」などの自己弁護や言い訳に捉えられる表現は一切使用しないこと。
* 指摘を受けた場合は、直ちに以下の3点を明確に述べること。
  1. 発生した問題の事実認定と謝罪。
  2. エラーを誘発した自らの見落とし・不作為の原因分析。
  3. 即座に実行する具体的な修正・対策内容。

---

## 3. コード品質および例外処理の事前検証ルール

### 3.1 エッジケースの考慮
* 文字列操作（`ColorFilter` 等での文字列置換）を実装する際は、単発の置換だけでなく、**「複数ワードが連続して除外された場合」**などのエッジケースを想定すること。
* カンマや空白が連続して残留する可能性を排除するため、ループ処理や正規表現の適用範囲を十分に吟味し、徹底的なクリーンアップ処理（再帰的なカンマ置換など）を最初から設計に組み込むこと。

### 3.2 プッシュ前のテスト実行
* 修正を行ったコードは、必ず事前にローカル環境またはテスト用スクリプト（`scratch/test_*.py` 等）を用いて動作検証を行い、期待通りの出力が得られることを確認した上でプッシュすること。

---

上記ルールは本プロジェクトの安定運用において不変のものであり、遵守を徹底すること。
